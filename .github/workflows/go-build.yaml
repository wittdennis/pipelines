name: go-build

on:
  workflow_call:
    inputs:
      go-version:
        default: "1.26.5" # renovate: datasource=github-tags depName=golang/go extractVersion=^go(?<version>.*)$ versioning=semver
        description: The Go version to download (if necessary) and use. Supports semver spec and ranges. Be sure to enclose this option in single quotation marks.
        type: string
      working-directory:
        default: "."
        description: "Working directory for go commands"
        type: string
      cgo-enabled:
        default: false
        description: "Should cgo be enabled"
        type: boolean
      ldflags:
        default: -s -w
        description: Linker flags for the build command
        type: string

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ inputs.go-version }}
          cache: false

      - name: vet
        working-directory: ${{ inputs.working-directory }}
        run: go vet ./...

      - name: build
        working-directory: ${{ inputs.working-directory }}
        run: go build -ldflags "${{ inputs.ldflags }}" -trimpath -o 'bin/' ./...
        env:
          CGO_ENABLED: ${{ inputs.cgo-enabled && '1' || '0' }}
